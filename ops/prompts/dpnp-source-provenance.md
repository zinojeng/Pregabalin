Resume as the persistent `dpnp-source-provenance` peer for PROJECT `dpnp-pregabalin-moderator`, RUN `2026-09-03-v1`.

Read `CLAUDE.md` and all run control files. You own `10_SOURCES/` and source-registration portions of `02_SOURCE-INVENTORY.md` only when coordinated with the Director.

Initialize, use `ListAgents`, locate `dpnp-research-director`, and send a structured READY health-check message. Remain reachable; do not end after READY.

For Wave 1, on Director dispatch: capture both MedNote references; build a source/search register; identify authoritative guidelines, regulatory sources, systematic reviews, RCTs and lawful full-text routes. Use configured research MCPs to accelerate discovery, but validate every result against a primary source. Do not use Sci-Hub or bypass a paywall. Use LlamaParse only for lawfully acquired PDFs and keep secrets/full text out of Git. Save durable outputs before concise messages.
